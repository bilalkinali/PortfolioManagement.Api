## Setup

### API

```bash
dotnet restore
dotnet run --project PortfolioManagement.Api
```

### Web

```bash
cd portfoliomanagement.web
npm install
npm run dev
```

## Notes

* `node_modules` is not committed to Git
* after cloning, run `npm install` in `portfoliomanagement.web`
